# 프로젝트 개요

2026 제1회 다솜 해커톤 출품작. 주제는 "사랑과 기술".

**동아리 전용 멘토-멘티 매칭 웹서비스.** 프로필 기반 알고리즘으로 재학생(멘토)과 신입생(멘티)을 1:1 매칭하고, 미션을 함께 수행하며 관계를 이어가게 만든다.

3인 팀, 밤샘 작업 중. 팀원 모두 코딩 경험이 오래되어 익숙하지 않음.

- 기술 스택: [ 백엔드 프레임워크/버전: FastAPI 0.135.3 (uvicorn 0.43.0로 구동, pip freeze 기준)
DB/ORM: SQLite(mentoring.db) + SQLAlchemy ORM (단, sqlalchemy 패키지 자체는 아직 미설치 상태)
화면 렌더링: 서버 템플릿 아님 — frontend/ 정적 HTML/CSS/JS가 별도로 존재하며 fetch로 백엔드 API 호출하는 분리형 구조
모델/테이블: User(users), Match(matches), Mission(missions) 3개 — 유저/매칭/미션 구조로 이미 구현돼 있음
실행 명령어: 별도 스크립트/README 없음 — 통상 uvicorn main:app --reload(backend 폴더 안에서)로 추정, 확인 필요]
- 개발 마감: 8/8(토) 08:40 (깃허브 최종 푸시) — 09:00 결과물 제출, 10:00 발표
- **발표 시간 내에 실제 시연을 하지 못하면 완성도 점수가 0점 처리된다**
- 평가 기준: 완성도 25% / 창의성 25% / 주제적합성 20% / 실현가능성 20% / 발표 10%

# 내 담당 범위

나는 **미션 시스템 전담**이다. `mission-branch`에서 작업한다. 기본 브랜치는 `master`다.

**담당**
- 미션 목록 데이터 및 진행 상태 관리 (잠금 / 진행중 / 완료)
- 사진 인증 처리
- **DASOM 경로 진행도 화면 + 클리어 이펙트** ← 창의성 점수의 핵심

**담당 아님 (다른 팀원 영역, 수정 금지)**
- 회원가입 / 로그인 / 프로필 작성
- 매칭 알고리즘 / 매칭 근거 카드

다른 팀원 영역의 코드가 필요하면 직접 수정하지 말고, 어떤 데이터가 필요한지 알려줄 것.

# 미션 설계 (확정)

## 전제

개강 후(9월) 운영을 전제로 한다. 대면 미션이 기본이며, **인증은 전부 사진 업로드 한 종류로 통일한다.** 인증 방식을 늘리면 화면을 종류마다 만들어야 해서 시간이 안 맞는다.

## 미션 목록 (총 10개)

**1단계 — 첫 만남 (필수, 1개)**

| # | 미션 | 인증 |
|---|---|---|
| 0 | 처음 만나서 같이 밥먹고 사진 찍기 | 사진 |

이걸 완료해야 2단계 전체가 해금된다.

**2단계 — 추억 쌓기 (9개, 순서 자유)**

| # | 미션 | 인증 |
|---|---|---|
| 1 | 학식 같이 먹기 | 사진 |
| 2 | 카페에서 같이 과제하기 | 사진 |
| 3 | 학교 상징물 앞 인증샷 | 사진 |
| 4 | 인생네컷 찍기 | 사진 |
| 5 | 술집에서 소주와 함께 사진 | 사진 |
| 6 | 학교 근처 맛집 정복하기 | 사진 |
| 7 | 같이 운동하기 | 사진 |
| 8 | 시험기간 같이 공부하기 | 사진 |
| 9 | 서로 엽기적인 사진 찍어주기 | 사진 |

2단계는 순서가 없다. 팀마다 다른 순서로 깨므로 피드가 다양해진다.

## 진행도 표현 — DASOM 경로 (메인 화면)

- 지도 위에 **DASOM** 글자 모양의 산책로가 흐릿하게 깔려 있다
- 미션을 완료할 때마다 캐릭터가 다음 구간으로 이동하고, 지나온 자리에 선이 진하게 그려진다
- **총 10개 미션 = 경로 10등분.** 진행도는 `완료 개수 / 10`
- 10개 전부 완료 → DASOM 글자 완성 + 반짝임 + 완성 메시지

**구현 방식**
- SVG `<path>` + `stroke-dasharray` / `stroke-dashoffset`으로 선 그리기 제어
- 캐릭터는 곡선을 정밀하게 따라가게 만들지 말 것. **구간 끝 좌표 배열을 미리 두고 `transform`으로 이동**시킨다. `offset-path`는 브라우저 호환성 이슈가 있어 쓰지 않는다
- 잠긴 미션도 흐릿하게 보여서 "다음에 뭐가 있는지" 기대감이 생기게 한다

## 클리어 순간 이펙트

미션 완료 시 **약 0.8초짜리 연출** 후 경로가 다음 구간까지 그려진다.

**이펙트는 반드시 교체 가능한 구조로 만든다.**
- 이펙트 함수를 레지스트리 객체에 등록하는 형태 (`EFFECTS = { stamp: fn, confetti: fn, ... }`)
- 함수 시그니처를 통일해서 새 이펙트 추가가 함수 하나 작성으로 끝나야 한다
- 현재 어떤 이펙트를 쓸지는 설정값 한 줄로 바꿀 수 있어야 한다
- 기본값은 도장(stamp). 트렌디한 대안(컨페티, 네온 펄스 등)을 함께 넣어두고 비교해서 고른다

**모든 연출은 CSS 애니메이션과 SVG 수준으로만 구현한다.** 무거운 라이브러리나 3D는 무대에서 깨질 위험이 있어 쓰지 않는다.

# 개인정보 원칙

- 미션 인증 사진은 로그인한 동아리원만 조회 가능
- 전체 피드에는 이름과 재학 상태만 노출 (연락처, 나이 비노출)
- 더미데이터는 실명·실제 번호 사용 금지

# 기능 우선순위

시간이 부족해지면 아래에서부터 자른다.

- **필수**: 미션 목록 조회, 1단계 해금 로직, 미션 완료 처리, DASOM 진행도 화면
- **중요**: 사진 업로드 인증, 클리어 이펙트, 전체 피드
- **여유되면**: 이펙트 고도화, 완성 연출, 양방향 확인 인증, 랭킹

**백업안**: DASOM SVG 경로가 새벽까지 안 풀리면 미련 없이 **폴라로이드 격자(빈 프레임 10칸이 사진으로 채워지는 방식)**로 갈아탄다. 데이터 구조가 같아서 화면만 교체하면 된다.

# 작업 방식

- `mission-branch`에서 작업. master 최신 반영은 `git pull --rebase origin master`
- DB 스키마와 패키지 설정 파일은 공용 — 수정이 필요하면 직접 고치지 말고 먼저 알릴 것
- DB 스키마는 초반에 확정했으며 변경 금지 (변경 시 3명 코드가 동시에 깨짐)

# 응답 방식

- **간결하게.** 밤샘 중이라 긴 글을 읽을 시간이 없다. 서론이나 요약 반복 없이 바로 본론으로
- 설명보다 **동작하는 코드를 먼저** 주고, 설명은 짧게 뒤에 붙인다
- **새로운 기능을 먼저 제안하지 마라.** 범위를 넓히는 조언보다 지금 있는 걸 완성시키는 조언이 필요하다. 기능 추가는 물어봤을 때만
- 시간이 부족해 보이는 선택지는 솔직하게 "이건 남은 시간에 무리"라고 말할 것
- 구현 방식을 고를 때는 **시연 성공 가능성**을 최우선 기준으로 판단할 것. 화려하지만 무대에서 깨질 수 있는 것보다, 단순하지만 확실히 돌아가는 쪽을 택한다
- 에러 대응 시 원인 추정을 나열하지 말고, **가장 가능성 높은 원인 하나와 확인 방법**부터 제시할 것
- 질문이 필요하면 한 번에 하나만
- 데모용 코드다. 과도한 주석이나 방어 로직 없이 동작을 최우선으로

# 주의사항

- **깃허브 링크 제출 이후 추가 커밋이 발견되면 무효 처리된다.** 08:40 이후 커밋 금지
- 발표자료 제출이 늦은 팀부터 역순으로 발표 순서가 배정된다